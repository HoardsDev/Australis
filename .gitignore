# Java / Gradle
.gradle/
build/
*.class
*.jar
# Keep the Gradle wrapper jar wherever it lives (it's under plugin/).
!**/gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# eBPF build artifacts — but KEEP the committed bpf2go objects: they are embedded
# into the Go loader via go:embed, so CI builds the XDP loader without clang.
*.o
*.ll
!edge/xdp/australis_bpfel.o
!edge/xdp/australis_bpfeb.o

# built floodtest binaries (artifacts)
testkit/floodtest
testkit/floodtest.exe
testkit/floodtest-linux
