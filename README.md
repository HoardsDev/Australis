# Australis built jars

Auto-published from main (5e2162a42947c11421bfe10c1a2473737676f6bb).

- `australis-velocity-*.jar` — drop into a Velocity proxy `plugins/`
- `australis-bungee-*.jar` — drop into a BungeeCord/Waterfall proxy `plugins/`
- `australis-paper-*.jar` — drop into a Paper/Spigot server `plugins/`
- `australis-limbo-*.jar` — drop into the Paper server you use as the verification limbo
